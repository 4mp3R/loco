<div class="page">
    <div class="container page-content">

        <h1 class="page-heading">Locali  <a class="button button-selected float-right" href="
            <?= $this->url(array(
                'controller' => 'accomodation',
                'action' => 'add'
            ), 'default', true); ?>
        ">Nuovo annuncio</a></h1>

        <div class="grid">

            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Titolo</th>
                        <th>Data inserimento</th>
                        <th>Indirizzo</th>
                        <th>Numero interessati</th>
                        <th>Operazioni</th>
                    </tr>
                </thead>
                <tbody>
                <?php foreach($this->data as $d): ?>
                    <tr>
                        <td><?= $d['accomodation']->title; ?></td>
                        <td><?= $d['accomodation']->created; ?></td>
                        <td><?= $d['accomodation']->address; ?></td>
                        <td><?= count($d['options']); ?></td>
                        <td>
                            <a class="button" href="
                                <?= $this->url(array(
                                'controller' => 'accomodation',
                                'action' => 'get',
                                'id' => $d['accomodation']->id
                            ), 'default', true); ?>
                            ">Dettagli</a>
                        </td>
                    </tr>
                <?php endforeach; ?>
                </tbody>
            </table>

        </div>
    </div>
</div>